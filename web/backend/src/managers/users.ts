// Users manager removed — password-based authentication is gone.
// Authentication is now entirely via Google OAuth (id_token verification).
// No passwords are stored anywhere in the backend.
// This file is kept as a stub to avoid import errors during transition.
export {};
